#include "main.h"
#include <stdio.h>

/**
 * print_array - print array
 * @a; pointer a
 * @n; contain length of array
 *
 * Return: void
 */
void print_array(int *a, int n)
{
	int i;
	array[n];
	a = array;

	for (i = 0; i < n; i++)
	{
		printf("%d, \n", *(array + i));
	}
}
