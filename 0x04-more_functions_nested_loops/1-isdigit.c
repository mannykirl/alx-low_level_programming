#include "main.h"
/**
 * _isdigit - return 0 for digit
 * @c: c is checked for digit
 *
 * Return: return 0 if condition not met
 */
int _isdigit(int c)
{
	c = 0;
	
	if (c <= 9)
	{
		return (1);
		c++;
	}
	else
	{
		retyrn (0);
	}
	return (0);
}
